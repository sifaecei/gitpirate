#mod key
set $mod Mod4
#Mod4 = Win
#Mod1 = Alt

font pango:Hack 10

#autostart
#exec_always --no-startup-id setxkbmap -option "grp:ctrl_shift_toggle" "us,ru"
exec --no-startup-id dunst
exec --no-startup-id clipmenud
exec --no-startup-id flameshot
exec --no-startup-id kbd_tray
exec --no-startup-id caffeine
exec_always --no-startup-id feh --bg-fill /home/stayasinic/.wall.jpg
exec --no-startup-id keepassxc --minimized
#exec --no-startup-id /home/stayasinic/.screenlayout/dual.sh
#exec --no-startup-id /home/stayasinic/.screenlayout/one.sh

#exec --no-startup-id i3-msg "workspace 1; exec xfce4-terminal; workspace 10; exec xfce4-terminal; workspace 2; exec firefox; workspace 6; exec firefox; workspace 1"

# Убить окно нажав средней кнопкой мыши на титульный заголовок окна
bindsym --release button2 kill

#перетаскивать окно за тайтлбар
tiling_drag modifier titlebar

#фокус следует за мышкой
focus_follows_mouse yes


#не фокусировать новое окно
no_focus [title="org.gnome.FileRoller"]
no_focus [title="Xarchiver"]
no_focus [window_role="pop-up"]

#не отображать название окна
for_window [tiling] border pixel 4

#отображение окна
for_window [all] border pixel 4

#отображать только для определённого
#for_window [class="xfce4-terminal"] border normal 4

#показать иконку в тайтле
#for_window [all] title_window_icon on

#сбрасывать вид окон на стек
#for_window [class=".*"] layout toggle split

#hotkey run app
bindsym Mod1+h exec --no-startup-id thunar
bindsym Mod1+w exec --no-startup-id firefox
bindsym Mod1+v exec --no-startup-id "alacritty -e vifm"
bindsym Print exec --no-startup-id flameshot gui
bindsym Control+F9 exec --no-startup-id pavucontrol
bindsym Mod1+m exec --no-startup-id thunderbird
bindsym Mod1+Pause exec --no-startup-id "sleep 1 && xset dpms force suspend"
#bindsym Mod1+v exec --no-startup-id /home/stayasinic/.scripts/dmenu/nmcli.sh
#bindsym Mod1+p exec --no-startup-id /home/stayasinic/.scripts/dmenu/mpc.sh
#bindsym Mod1+a exec --no-startup-id /home/stayasinic/.scripts/audioswitch.sh
bindsym Mod1+t exec --no-startup-id /opt/translator/translate_selection.sh


# start a terminal
bindsym $mod+Return exec alacritty

# program launchers
bindsym $mod+d exec --no-startup-id dmenu_run -b -i -fn "hack-12" -nb '#2D323D' -nf '#ffffff' -sb '#5294e2'
bindsym $mod+c exec --no-startup-id clipmenu -b -i -l 20 -fn "hack-12" -nb '#2D323D' -nf '#ffffff' -sb '#5294e2'
bindsym Mod1+d exec --no-startup-id rofi -font "hack 10" -show drun



#power contol
bindsym Control+Mod1+Delete mode "$mode_system"
set $mode_system System (s)uspend, (r)eboot, (p)oweroff
mode "$mode_system" {
    bindsym s exec --no-startup-id systemctl suspend, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym p exec --no-startup-id systemctl poweroff -i, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

#floating size
floating_minimum_size 80 x 60
floating_maximum_size 1920 x 1080

#window floating
for_window [window_role="task_dialog"] floating enable
for_window [title="Preferences$"] floating enable
for_window [title="Громкость"] floating enable
for_window [title="Действия над файлами"] floating enable
for_window [title="Сетевые соединения"] floating enable
for_window [title="org.gnome.FileRoller"] floating enable
for_window [title="Извлечь"] floating enable
for_window [title="Color Picker"] floating enable
for_window [title="Translation"] floating enable

# switch workspaces by mod+ctrl+left/right
bindsym $mod+Control+Left workspace prev
bindsym $mod+Control+Right workspace next

#scripts
bindsym Mod1+Shift+f exec --no-startup-id /home/stayasinic/.scripts/find.sh
bindsym Mod1+Shift+t exec --no-startup-id /home/stayasinic/.scripts/findrt.sh

#bindsym XF86AudioRaiseVolume exec --no-startup-id  /home/stayasinic/.scripts/up.sh
#bindsym XF86AudioLowerVolume exec --no-startup-id  /home/stayasinic/.scripts/down.sh
#bindsym XF86AudioMute exec --no-startup-id /home/stayasinic/.scripts/mute.sh

bindsym Control+F12 exec --no-startup-id  /home/stayasinic/.scripts/up.sh
bindsym Control+F11 exec --no-startup-id  /home/stayasinic/.scripts/down.sh
bindsym Control+F10 exec --no-startup-id /home/stayasinic/.scripts/mute.sh

# Media player controls
#bindsym XF86AudioPlay exec --no-startup-id "mpc toggle"
#bindsym XF86AudioNext exec --no-startup-id "mpc next"
#bindsym XF86AudioPrev exec --no-startup-id "mpc prev"
#bindsym XF86AudioStop exec --no-startup-id "mpc stop"

#bindsym Mod1+Up exec --no-startup-id "mpc --host=192.168.1.10 toggle"
#bindsym Mod1+Right exec --no-startup-id "mpc --host=192.168.1.10 next"
#bindsym Mod1+Left exec --no-startup-id "mpc --host=192.168.1.10 prev"
#bindsym Mod1+Down exec --no-startup-id "mpc --host=192.168.1.10 stop"

#xprop | grep WM_CLASS
#start on desktop

assign [class="firefox"] $ws2
assign [class="cr3"] $ws7
#assign [class="djview"] $ws7
assign [class="zathura"] $ws7
assign [class="Readest"] $ws7
assign [class="readest"] $ws7
assign [class="MComix"] $ws7
assign [class="org.mozilla.Thunderbird"] $ws8
assign [class="qBittorrent"] $ws9
#assign [class="mpv"] $ws10
#for_window [class="qBittorrent"] move to workspace $ws9

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# kill focused window
bindsym Mod1+F4 kill

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

#vim style
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right
bindsym $mod+Shift+Left move left

# split toggle
bindsym $mod+x split toggle

# split in horizontal orientation
#bindsym $mod+h split v

# split in vertical orientation
#bindsym $mod+v split h

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking

#переключаем в таб режим
bindsym $mod+w layout tabbed

#переключаем в сплит режим
bindsym $mod+e layout toggle split

#переключаем режимы
#bindsym $mod+w layout toggle

# float
bindsym $mod+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+Shift+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+b focus child

#scratchpad
# Make the currently focused window a scratchpad
bindsym $mod+minus move scratchpad

# Show the first scratchpad window
bindsym $mod+equal scratchpad show

# Show the sup-mail scratchpad window, if any.
#bindsym mod4+s [title="^Sup ::"] scratchpad show

# Перемещение окна на рабочий стол через dmenu
#bindsym $mod+g exec "dmenu -b -i -fn 'hack-12' -nb '#2D323D' -nf '#ffffff' -sb '#5294e2' -p 'Go to:' < /dev/null | xargs -I{} i3-msg workspace {}"

#bindsym $mod+Shift+g exec "dmenu -b -i -fn 'hack-12' -nb '#2D323D' -nf '#ffffff' -sb '#5294e2' -p 'Move to:' < /dev/null | xargs -I{} i3-msg move container to workspace {}"

#bindsym $mod+g exec "printf '%s\n' '1:terminal' '2:browser' '3:chats' '4:files' '5:random' '6:random' '7:files' '8:misk' '9:browser' '10:terminal' '11:mail' '12:torrent' | dmenu -b -i -fn 'hack-12' -nb '#2D323D' -nf '#ffffff' -sb '#5294e2' -p 'Go to:' | xargs -r i3-msg workspace"

#bindsym $mod+Shift+g exec "printf '%s\n' '1:terminal' '2:browser' '3:chats' '4:files' '5:random' '6:random' '7:files' '8:misk' '9:browser' '10:terminal' '11:mail' '12:torrent' | dmenu -b -i -fn 'hack-12' -nb '#2D323D' -nf '#ffffff' -sb '#5294e2' -p 'Move to:' | xargs -r -I{} i3-msg move container to workspace '{}'"

#workspace $ws1 output HDMI1
#workspace $ws2 output HDMI1
#workspace $ws3 output HDMI1
#workspace $ws4 output HDMI1
#workspace $ws5 output HDMI1
#workspace $ws6 output VGA1
#workspace $ws7 output VGA1
#workspace $ws8 output VGA1
#workspace $ws9 output VGA1
#workspace $ws10 output VGA1

####named
set $ws1 "1:terminal"
set $ws2 "2:browser"
set $ws3 "3:chats"
set $ws4 "4:files"
set $ws5 "5:random"
set $ws6 "6:remote"
set $ws7 "7:reader"
set $ws8 "8:mail"
set $ws9 "9:torrent"
set $ws10 "10:media"

###clean
#set $ws1 1
#set $ws2 2
#set $ws3 3
#set $ws4 4
#set $ws5 5
#set $ws6 6
#set $ws7 7
#set $ws8 8
#set $ws9 9
#set $ws10 10

# Переключение рабочих столов
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10


# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 5 px or 5 ppt
        bindsym k resize grow height 5 px or 5 ppt
        bindsym j resize shrink height 5 px or 5 ppt
        bindsym l resize grow width 5 px or 5 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 5 px or 5 ppt
        bindsym Up resize grow height 5 px or 5 ppt
        bindsym Down resize shrink height 5 px or 5 ppt
        bindsym Right resize grow width 5 px or 5 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)

bar {
   status_command i3status
#    status_command /home/stayasinic/.scripts/layout.sh
    workspace_min_width 140
    bindsym button5 nop
    bindsym button4 nop
    position bottom
#    tray_output primary

#    mode hide
#    hidden_state hide
#    modifier Mod4+Control

#    tray_output DP-4
     colors {
        background #2d323d
        statusline #ffffff
                           #border backgr  text
        focused_workspace  #545b6c #545b6c #ffffff
        active_workspace   #868686 #868686 #ffffff
        inactive_workspace #2d323d #2d323d #ffffff
        urgent_workspace   #5294e2 #5294e2 #ffffff
        }
}


#colors
#class                  border    backgr    text      indicator child_border
client.focused          #545B6C   #545B6C   #ffffff   #5294e2   #545B6C
client.focused_inactive #222222   #222222   #ffffff   #5294e2   #222222
client.unfocused        #222222   #222222   #ffffff   #5294e2   #222222
client.urgent           #2f343a   #5294e2   #ffffff   #5294e2   #5294e2
client.placeholder      #222222   #222222   #5294e2   #5294e2   #222222
client.background       #444a58
